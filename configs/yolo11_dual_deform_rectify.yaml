# Ultralytics YOLO 🚀, AGPL-3.0 license
# EXPERIMENT 10: ULTIMATE FUSION
# Architecture: Dual-Stream Backbone + Deformable Rectify Fusion (DCN + CMX + ECA)
# Strategy:
#   1. DCN: Căn chỉnh đặc trưng (Alignment)
#   2. Rectify: Sửa lỗi chéo (Correction)
#   3. ECA: Chọn lọc kênh (Attention)

# Parameters
nc: 4 # Số lớp (Perilla, Melon, etc.) -
scales:
  custom: [1.00, 1.00, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, InputContainer, []] # 0

  # === BRANCH 1: RGB (YOLOv11s Source) ===
  - [0, 1, Conv, [32, 3, 2]] # 1
  - [-1, 1, Conv, [64, 3, 2]] # 2
  - [-1, 1, C3k2, [128, False, 0.25]] # 3
  - [-1, 1, Conv, [128, 3, 2]] # 4
  - [-1, 1, C3k2, [256, False, 0.25]] # 5 (P3 RGB)
  - [-1, 1, Conv, [256, 3, 2]] # 6
  - [-1, 1, C3k2, [256, True, 0.25]] # 7 (P4 RGB)
  - [-1, 1, Conv, [512, 3, 2]] # 8
  - [-1, 1, C3k2, [512, True, 0.25]] # 9
  - [-1, 1, SPPF, [512, 5]] # 10 (P5 RGB)

  # === BRANCH 2: NIR/NDVI (YOLOv11n Source) ===
  - [0, 1, Conv, [16, 3, 2]] # 11
  - [-1, 1, Conv, [32, 3, 2]] # 12
  - [-1, 1, C3k2, [64, False, 0.25]] # 13
  - [-1, 1, Conv, [64, 3, 2]] # 14
  - [-1, 1, C3k2, [128, False, 0.25]] # 15 (P3 NIR)
  - [-1, 1, Conv, [128, 3, 2]] # 16
  - [-1, 1, C3k2, [128, True, 0.25]] # 17 (P4 NIR)
  - [-1, 1, Conv, [256, 3, 2]] # 18
  - [-1, 1, C3k2, [256, True, 0.25]] # 19
  - [-1, 1, SPPF, [256, 5]] # 20 (P5 NIR)

head:
  # === ULTIMATE FUSION NECK ===
  # Thay thế FusionRectify bằng FusionDeformRectify

  # --- Fusion P3 ---
  # RGB(256) + NIR(128). Nâng NIR lên 256 để khớp channels
  - [15, 1, Conv, [256, 1, 1]] # 21 (Align channel NIR -> 256)
  - [[5, 21], 1, FusionDeformRectify, []] # 22 (Ultimate Fusion P3)

  # --- Fusion P4 ---
  # RGB(256) + NIR(128). Nâng NIR lên 256
  - [17, 1, Conv, [256, 1, 1]] # 23 (Align channel NIR -> 256)
  - [[7, 23], 1, FusionDeformRectify, []] # 24 (Ultimate Fusion P4)

  # --- Fusion P5 ---
  # RGB(512) + NIR(256). Nâng NIR lên 512
  - [20, 1, Conv, [512, 1, 1]] # 25 (Align channel NIR -> 512)
  - [[10, 25], 1, FusionDeformRectify, []] # 26 (Ultimate Fusion P5)

  # === HEAD (Standard YOLOv11s Logic) ===
  # Phần này giữ nguyên để đảm bảo đầu ra giống hệt các bài trước
  - [26, 1, C3k2, [512, False]] # 27

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 28
  - [[-1, 24], 1, Concat, [1]] # 29
  - [-1, 1, C3k2, [256, False]] # 30

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 31
  - [[-1, 22], 1, Concat, [1]] # 32
  - [-1, 1, C3k2, [128, False]] # 33

  - [-1, 1, Conv, [128, 3, 2]] # 34
  - [[-1, 30], 1, Concat, [1]] # 35
  - [-1, 1, C3k2, [256, False]] # 36

  - [-1, 1, Conv, [256, 3, 2]] # 37
  - [[-1, 27], 1, Concat, [1]] # 38
  - [-1, 1, C3k2, [512, True]] # 39

  - [[33, 36, 39], 1, Detect, [nc]] # 40
